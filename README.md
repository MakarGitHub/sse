# GoLang Server-Sent-Events (SSE) Package #

This package provides a simple implementation of Server-Sent Events (SSE) in Go.

## Installation ##

To install the package, run the following command:

```sh
go get -u github.com/MakarGitHub/sse
```